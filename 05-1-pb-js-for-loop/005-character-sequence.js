function characterSequence(input) {
    let text = input;
    let length = text.length;

    for (i = 0; i < length; i++) {
        console.log(text[i]);
    }
}

characterSequence("Simeon Atanasov");